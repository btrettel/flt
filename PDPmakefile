# Public domain POSIX make on Linux
# <https://frippery.org/make/>
# Works with `pdpmake -f PDPmakefile`.
# Intended mostly to make sure that common.mk and depends.mk are POSIX compliant.

.POSIX:

.PRAGMA: target_name posix_202x

# defaults
BUILD = debug
include mk/linux_1.mk

#############
# Compilers #
#############

# Only gfortran with BUILD=debug for now.
include mk/gfortran.mk
FFLAGS ::= $(FFLAGS) $(DBGFLAGS)

include mk/common.mk
include mk/depends.mk
include mk/linux_2.mk
